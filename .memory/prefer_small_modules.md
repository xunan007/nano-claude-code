---
name: prefer_small_modules
description: User prefers small, focused TypeScript modules over large monolithic files.
type: user
---
The user prefers TypeScript code organized into clear, small modules rather than putting all logic in one large file. When writing or refactoring TypeScript code, I should favor extracting separate files/modules for distinct concerns.
